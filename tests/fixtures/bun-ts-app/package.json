{
  "name": "bun-ts-fixture",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test",
    "lint": "biome check ."
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/bun": "latest"
  }
}
